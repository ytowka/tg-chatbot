aiogram==3.29.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# OpenAI-compatible async-клиент. Указывает на llama-server (через SSH tunnel)
# или на облачный API — зависит от LLM_BASE_URL в .env.
openai>=1.0

# IANA timezone DB для zoneinfo (нужен в slim-образах без системного tzdata).
tzdata>=2024.1
